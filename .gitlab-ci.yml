pages:
  stage: deploy
  tags:
    - runner3
  script:
    - echo "Site vitrine >>> $CI_PAGES_URL"
  artifacts:
    paths:
      - public
    expire_in: never
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_BRANCH == "develop"