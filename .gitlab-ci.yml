pages:
  tags:
    - runner3
  image: alpine:latest
  stage: deploy
  script:
    - mkdir public
    - cp index.html public/
    - cp style.css public/
    - cp -r src public/
  artifacts:
    paths:
      - public
  only:
    - main


